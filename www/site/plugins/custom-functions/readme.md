Create methods faster. All files added to one of the method folders create an eponymous method to the page/pages/site objet.

Warning: your files must return valid functions. No tests are done on import.
